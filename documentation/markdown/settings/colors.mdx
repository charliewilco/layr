import { Snippet } from '../../components/snippet'
import { Colors, snippet } from './demo/colors'

Colors are pretty barebones in Layr. They provide sensible defaults to get going with if you're prototyping but have the ability to be overridden. The documentation reads the built custom properties from Layr's local CSS variable export.

<Snippet component={Colors} snippet={snippet} />
